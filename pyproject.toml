[project]
name = "infrahub-check-via-graph-traversal-demo"
version = "0.1.0"
description = "Demo runner for the reachability-check pattern against Infrahub 1.10 + SDK 1.22."
requires-python = ">=3.10"
dependencies = [
  "infrahub-sdk[ctl]==1.22.0",
  "invoke>=2.2",
]

[tool.uv]
package = false
